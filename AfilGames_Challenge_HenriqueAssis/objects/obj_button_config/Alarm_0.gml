event_inherited();

var _buttons_layer = "Buttons";
var _configurations_layer = "Configurations";

instance_deactivate_layer(_buttons_layer);
instance_activate_layer(_configurations_layer);

with (obj_ui_manager)
{
	select_ui(inst_1ABD99EB);
}